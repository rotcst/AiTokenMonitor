<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        Dark menu palette matching the Claude tray menu. TrayIconService reads these same keys, so
        the tray and the window's context menu stay in step.
    -->
    <!-- Reference palette (Uiverse by Na3ar-17): dark gradient card, blue hover, purple accent group. -->
    <LinearGradientBrush x:Key="ModernMenuBackgroundBrush" StartPoint="0,0" EndPoint="0.85,1">
        <GradientStop Offset="0" Color="#FF242832" />
        <GradientStop Offset="1" Color="#FF251C28" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ModernMenuBorderBrush" Color="#FF42434A" />
    <SolidColorBrush x:Key="ModernMenuTextBrush" Color="#FF7E8590" />
    <SolidColorBrush x:Key="ModernMenuHoverTextBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="ModernMenuBlueHoverBrush" Color="#FF5353FF" />
    <SolidColorBrush x:Key="ModernMenuPurpleTextBrush" Color="#FFBD89FF" />
    <SolidColorBrush x:Key="ModernMenuPurpleHoverBrush" Color="#D5382D47" />
    <SolidColorBrush x:Key="ModernMenuRedHoverBrush" Color="#FF8E2A2A" />
    <SolidColorBrush x:Key="ModernMenuHoverBrush" Color="#FF2F343C" />
    <SolidColorBrush x:Key="ModernMenuSeparatorBrush" Color="#FF42434A" />
    <SolidColorBrush x:Key="ModernMenuMutedTextBrush" Color="#FF7E8590" />

    <Style x:Key="MenuIconStyle" TargetType="{x:Type Path}">
        <Setter Property="Width" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Fill" Value="Transparent" />
        <Setter Property="Stroke"
                Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type MenuItem}}}" />
        <Setter Property="StrokeThickness" Value="1.8" />
        <Setter Property="StrokeStartLineCap" Value="Round" />
        <Setter Property="StrokeEndLineCap" Value="Round" />
        <Setter Property="StrokeLineJoin" Value="Round" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="ModernContextMenuStyle" TargetType="{x:Type ContextMenu}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="MinWidth" Value="176" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Background" Value="{StaticResource ModernMenuBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ModernMenuBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource ModernMenuTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Microsoft YaHei UI, Segoe UI" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="HasDropShadow" Value="False" />
        <Setter Property="StaysOpen" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border x:Name="ModernMenuChrome"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16"
                                              Direction="270"
                                              ShadowDepth="3"
                                              Opacity="0.34"
                                              Color="#70000000" />
                        </Border.Effect>
                        <ScrollViewer CanContentScroll="True"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Height" Value="31" />
        <Setter Property="Margin" Value="0,1.5" />
        <Setter Property="Padding" Value="8,0" />
        <!-- Background carries the item's HOVER colour; the resting chrome stays transparent. -->
        <Setter Property="Background" Value="{StaticResource ModernMenuBlueHoverBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ModernMenuTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Microsoft YaHei UI, Segoe UI" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Grid>
                        <Border x:Name="ModernMenuItemChrome"
                                Background="Transparent"
                                CornerRadius="6"
                                RenderTransformOrigin="0.5,0.5"
                                SnapsToDevicePixels="True">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform x:Name="ItemScale" ScaleX="1" ScaleY="1" />
                                    <TranslateTransform x:Name="ItemSlide" X="0" Y="0" />
                                </TransformGroup>
                            </Border.RenderTransform>
                            <Grid>
                                <!-- Hover wash: its colour is the item's Background; opacity animates in. -->
                                <Border x:Name="HoverFill"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="6"
                                        Opacity="0" />
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="26" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="14" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="IconHost"
                                                      Grid.Column="0"
                                                      ContentSource="Icon"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                    <TextBlock x:Name="CheckGlyph"
                                               Grid.Column="0"
                                               Text="✓"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontFamily="Segoe UI Symbol"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="{TemplateBinding Foreground}"
                                               Visibility="Collapsed" />
                                    <ContentPresenter Grid.Column="1"
                                                      Margin="4,0,0,0"
                                                      VerticalAlignment="Center"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    <TextBlock x:Name="SubmenuArrow"
                                               Grid.Column="2"
                                               Text="›"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontFamily="Segoe UI Symbol"
                                               FontSize="13"
                                               Foreground="{TemplateBinding Foreground}"
                                               Visibility="Collapsed" />
                                </Grid>
                            </Grid>
                        </Border>
                        <!-- PART_Popup routes clicks to submenu children (e.g. the language choices). -->
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               Placement="Right"
                               PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                               PopupAnimation="Fade">
                            <Border MinWidth="150"
                                    Padding="8"
                                    Background="{StaticResource ModernMenuBackgroundBrush}"
                                    BorderBrush="{StaticResource ModernMenuBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16"
                                                      Direction="270"
                                                      ShadowDepth="3"
                                                      Opacity="0.34"
                                                      Color="#70000000" />
                                </Border.Effect>
                                <ScrollViewer CanContentScroll="True"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- IsHighlighted covers both mouse-over and keyboard selection. -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ModernMenuHoverTextBrush}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverFill"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.22" />
                                        <DoubleAnimation Storyboard.TargetName="ItemSlide"
                                                         Storyboard.TargetProperty="X"
                                                         To="1.2"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ItemSlide"
                                                         Storyboard.TargetProperty="Y"
                                                         To="-1.2"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverFill"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.18" />
                                        <DoubleAnimation Storyboard.TargetName="ItemSlide"
                                                         Storyboard.TargetProperty="X"
                                                         To="0"
                                                         Duration="0:0:0.18" />
                                        <DoubleAnimation Storyboard.TargetName="ItemSlide"
                                                         Storyboard.TargetProperty="Y"
                                                         To="0"
                                                         Duration="0:0:0.18" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ItemScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="0.985"
                                                         Duration="0:0:0.06" />
                                        <DoubleAnimation Storyboard.TargetName="ItemScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="0.985"
                                                         Duration="0:0:0.06" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ItemScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1"
                                                         Duration="0:0:0.1" />
                                        <DoubleAnimation Storyboard.TargetName="ItemScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1"
                                                         Duration="0:0:0.1" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                            <Setter TargetName="IconHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The window-control group (last list): purple text, purple-tinted hover, echoing the reference. -->
    <Style x:Key="ModernMenuItemAccentStyle"
           TargetType="{x:Type MenuItem}"
           BasedOn="{StaticResource ModernMenuItemStyle}">
        <Setter Property="Background" Value="{StaticResource ModernMenuPurpleHoverBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ModernMenuPurpleTextBrush}" />
    </Style>

    <Style x:Key="ModernMenuSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Margin" Value="10,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Height="1.5"
                            Background="{StaticResource ModernMenuSeparatorBrush}"
                            SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ContextMenu}"
           BasedOn="{StaticResource ModernContextMenuStyle}" />
    <Style TargetType="{x:Type MenuItem}"
           BasedOn="{StaticResource ModernMenuItemStyle}" />
    <Style TargetType="{x:Type Separator}"
           BasedOn="{StaticResource ModernMenuSeparatorStyle}" />

    <!-- Shared modern scroll bar: history window and the main window's detail panel. -->
    <Style x:Key="ScrollPageButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernScrollThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Background" Value="#5E7D8996" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="3" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#A59AA6B2" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="Background" Value="#D5B8C2CC" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="ModernVerticalScrollBarTemplate"
                     TargetType="{x:Type ScrollBar}">
        <Grid Background="Transparent">
            <Border x:Name="ModernRail"
                    Margin="3,4"
                    Background="#10FFFFFF"
                    CornerRadius="3" />
            <Track x:Name="PART_Track"
                   Margin="3,4"
                   IsDirectionReversed="True"
                   Orientation="Vertical"
                   Minimum="{TemplateBinding Minimum}"
                   Maximum="{TemplateBinding Maximum}"
                   Value="{TemplateBinding Value}"
                   ViewportSize="{TemplateBinding ViewportSize}">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                  Style="{StaticResource ScrollPageButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb MinHeight="32"
                           Style="{StaticResource ModernScrollThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                  Style="{StaticResource ScrollPageButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ModernHorizontalScrollBarTemplate"
                     TargetType="{x:Type ScrollBar}">
        <Grid Background="Transparent">
            <Border x:Name="ModernRail"
                    Margin="4,3"
                    Background="#10FFFFFF"
                    CornerRadius="3" />
            <Track x:Name="PART_Track"
                   Margin="4,3"
                   IsDirectionReversed="False"
                   Orientation="Horizontal"
                   Minimum="{TemplateBinding Minimum}"
                   Maximum="{TemplateBinding Maximum}"
                   Value="{TemplateBinding Value}"
                   ViewportSize="{TemplateBinding ViewportSize}">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}"
                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                  Style="{StaticResource ScrollPageButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb MinWidth="32"
                           Style="{StaticResource ModernScrollThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}"
                                  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                  Style="{StaticResource ScrollPageButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#7D8996" />
        <Setter Property="Width" Value="11" />
        <Setter Property="Template" Value="{StaticResource ModernVerticalScrollBarTemplate}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="11" />
                <Setter Property="Template" Value="{StaticResource ModernHorizontalScrollBarTemplate}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.35" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
